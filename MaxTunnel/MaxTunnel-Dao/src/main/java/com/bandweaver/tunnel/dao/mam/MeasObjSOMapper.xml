<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bandweaver.tunnel.dao.mam.MeasObjSOMapper">

    <insert id="insert" parameterType="com.bandweaver.tunnel.common.biz.pojo.mam.measobj.MeasObjSO">
        INSERT INTO T_MAM_MEASOBJ_SO (ID, REFRESH_TIME, cv)
        VALUES (#{id}, #{refreshTime}, #{cv})
    </insert>
    
    <insert id="insertSelective"  parameterType="com.bandweaver.tunnel.common.biz.pojo.mam.measobj.MeasObjSO">
        INSERT INTO T_MAM_MEASOBJ_SO
        <trim prefix="(" suffix=")" suffixOverrides=",">
        	<if test="id != null">
        		id,
        	</if>
        	<if test="refreshTime != null">
        		REFRESH_TIME,
        	</if>
        	<if test="cv != null">
        		cv,
        	</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
        	<if test="id != null">
        		#{id,jdbcType=DECIMAL},
        	</if>
        	<if test="refreshTime != null">
        		#{refreshTime,jdbcType=TIMESTAMP},
        	</if>
        	<if test="cv != null">
        		#{cv,jdbcType=DECIMAL},
        	</if>
        </trim>
    </insert>

    <update id="update" parameterType="com.bandweaver.tunnel.common.biz.pojo.mam.measobj.MeasObjSO">
        UPDATE T_MAM_MEASOBJ_SO
        SET REFRESH_TIME = #{refreshTime}, cv = #{cv}
        WHERE id = #{id} 
    </update>

    <select id="getMeasObjSO" resultMap="MeasObjSOMap">
        SELECT
            a.*,
            b.*
        FROM T_MAM_MEASOBJ_SO a
            LEFT JOIN t_mam_measobj b ON a.id = b.id
        WHERE a.id = #{id}
    </select>

    <select id="getAllMeasObjSOs" resultMap="MeasObjSOMap">
        SELECT
            a.*,
            b.*
        FROM T_MAM_MEASOBJ_SO a
            LEFT JOIN t_mam_measobj b ON a.id = b.id
    </select>

    <resultMap id="MeasObjSOMap" type="com.bandweaver.tunnel.common.biz.pojo.mam.measobj.MeasObjSO">
        <id column="ID" jdbcType="DECIMAL" property="id"/>
        <result column="section_id" jdbcType="DECIMAL" property="sectionId"/>
        <result column="NAME" jdbcType="VARCHAR" property="name"/>
        <result column="DATATYPE_ID" jdbcType="DECIMAL" property="datatypeId"/>
        <result column="OBJTYPE_ID" jdbcType="DECIMAL" property="objtypeId"/>
        <result column="ACTIVED" jdbcType="DECIMAL" property="actived"/>
        <result column="DESCRIPTION" jdbcType="VARCHAR" property="description"/>
        <result column="LONGITUDE" jdbcType="VARCHAR" property="longitude"/>
        <result column="LATITUDE" jdbcType="VARCHAR" property="latitude"/>
        <result column="height" jdbcType="VARCHAR" property="height"/>
        <result column="DEVIATION" jdbcType="DECIMAL" property="deviation"/>
        <result column="REFRESH_TIME" jdbcType="VARCHAR" property="refreshTime"/>
        <result column="cv" jdbcType="VARCHAR" property="cv"/>
    </resultMap>

	 <insert id="addBatch" useGeneratedKeys="false">
        insert into T_MAM_MEASOBJ_SO
        (ID, REFRESH_TIME, cv)
        select a.* from (
        <foreach collection="list" item="item" index="index" close=")"
                 open="(" separator="union">
            select
            #{item.id},
            #{item.refreshTime},
            #{item.cv}
            from dual
        </foreach>
        )a
    </insert>

<delete id="deleteByPrimaryKey">
	delete from T_MAM_MEASOBJ_SO where id = #{id}
</delete>

	<select id="getByCondition" resultMap="dtoResultMap">
		select * from T_MAM_MEASOBJ_SO
		<where>
			<if test="id != null">
				and id = #{id}
			</if>
			<if test="ids != null">
				and id in
				<foreach collection="ids" item="item" index="index"
					close=")" open="(" separator=",">
					#{item}
				</foreach>
			</if>
		</where>
		order by refresh_time desc
	</select>
	
	<select id="getMeasObjSOByCondition" resultMap="dtoResultMap">
        select t.* from T_MAM_MEASOBJ_SO t
        left join T_MAM_MEASOBJ m on t.id = m.id
        <where>
            <if test="id != null">
                and t.id = #{id,jdbcType=DECIMAL}
            </if>
            <if test="tunnelId != null">
                and m.tunnel_id = #{tunnelId,jdbcType=DECIMAL}
            </if>
            <if test="storeId != null">
                and m.store_id = #{storeId,jdbcType=DECIMAL}
            </if>
            <if test="areaId != null">
                and m.area_id = #{areaId,jdbcType=DECIMAL}
            </if>
            <if test="sectionId != null">
                and m.SECTION_ID = #{sectionId,jdbcType=DECIMAL}
            </if>
            <if test="name != null and name.trim() != ''">
               and m.instr(name,#{name})>0
            </if>
            <if test="datatypeId != null">
                and m.DATATYPE_ID = #{datatypeId,jdbcType=DECIMAL}
            </if>
            <if test="objtypeId!= null">
            	and m.OBJTYPE_ID = #{objtypeId,jdbcType=DECIMAL}
            </if>
            <if test="objtypeIds != null">
             and m.OBJTYPE_ID in 
             <foreach collection="objtypeIds" item="item" index="index" close=")"
                 open="(" separator=",">
                 #{item}
             </foreach>
            </if>
            <if test="tunnelIds != null">
             and m.tunnel_id in 
             <foreach collection="tunnelIds" item="item" index="index" close=")"
                 open="(" separator=",">
                 #{item}
             </foreach>
            </if>
            <if test="storeIds != null">
             and m.store_id in 
             <foreach collection="storeIds" item="item" index="index" close=")"
                 open="(" separator=",">
                 #{item}
             </foreach>
            </if>
            <if test="sectionIds != null">
             and m.SECTION_ID in 
             <foreach collection="sectionIds" item="item" index="index" close=")"
                 open="(" separator=",">
                 #{item}
             </foreach>
            </if>
        </where>
        order by t.refresh_time desc
    </select>
    
	<resultMap id="dtoResultMap" type="com.bandweaver.tunnel.common.biz.dto.mam.MeasObjSODto">
        <id column="ID" jdbcType="DECIMAL" property="id"/>
        <result column="REFRESH_TIME" jdbcType="VARCHAR" property="refreshTime"/>
        <result column="cv" jdbcType="DECIMAL" property="cv"/>
        <association property="obj" javaType="com.bandweaver.tunnel.common.biz.dto.mam.MeasObjDto" column="id"
        	select="com.bandweaver.tunnel.dao.mam.MeasObjMapper.getDtoById"/>
    </resultMap>
</mapper>