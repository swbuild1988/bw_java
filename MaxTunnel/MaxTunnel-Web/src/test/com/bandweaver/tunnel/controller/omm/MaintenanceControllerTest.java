package com.bandweaver.tunnel.controller.omm;

import com.bandweaver.tunnel.common.platform.config.SystemConfigService;
import org.junit.Test;

import static org.junit.Assert.*;

public class MaintenanceControllerTest {

    @Test
    public void getDefectsByCondition() {
        Object type = null;
        String s = String.valueOf(type);
        System.out.println(s);

        int k = ((Integer)type).intValue();
        System.out.println(k);
    }
}